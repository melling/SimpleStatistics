    import XCTest
    @testable import SimpleStatistics

    final class SimpleStatisticsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SimpleStatistics().text, "Hello, World!")
        }
    }
